model:
  tokenizer_path: kuelumbus/polyBERT

  params:
    latent_dim: 2560
    d_model: 2560
    nhead: 32
    dim_feedforward: 5120
    activation: "gelu"
    dropout: 0.0
    norm_first: true
    bias: true
    num_layers: 58
