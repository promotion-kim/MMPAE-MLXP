model:
  tokenizer_path: kuelumbus/polyBERT

  params:
    latent_dim: 2048
    d_model: 2048
    nhead: 32
    dim_feedforward: 4096
    activation: "gelu"
    dropout: 0.0
    norm_first: true
    bias: true
    num_layers: 40
